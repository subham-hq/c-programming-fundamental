# Assets

Place image assets here and reference them from the README.

These are **placeholders** — no images are committed yet. Suggested captures to add:

| Filename | Suggested content |
|----------|-------------------|
| `banner.png` | A simple repository banner/title card |
| `terminal-prime-check.png` | Screenshot of `prime_check` running |
| `terminal-planet-habitability.png` | Screenshot of the planet-habitability menu |
| `ci-passing.png` | The green Actions build (optional; the README already links a live badge) |

Keep images small (compress PNGs) so the repository stays lightweight.
